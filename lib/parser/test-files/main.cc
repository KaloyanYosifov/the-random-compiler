int a = 3;
int b = 5;
int c = a;
bool a_bool = true; 
bool another_bool = true && false || true || false; 
bool another_bool_test = (1 + 2) && ((4 + 2) / (3 * 1)) || (a + b);
string test = "Hello there" + "Hey brother :wave";
string testing = "hello world!";

if (a > b && b < 4) {
    bool condition_bool = false;
}

while (a > b && b < 4) {
    if (3 > 5) {
        System.print("hello there!");
    }
}

for (int i = 0; i < 5; i ++) {
    System.print("hello there!");
    int r = 3;
    int t = 5;
    int v = a;
    bool a_test = (1 + 2) && ((4 + 2) / (3 * 1)) || (a + b);

    if (r > t && v < 4) {
        bool condition_bool = false;
    }

    while (r > t && v < 4) {
        if (3 > 5) {
            System.print("hello there!");
        }
    }
}

System.print(a + b);
System.print(c && testing);
System.print(c || testing);
